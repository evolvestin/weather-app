from pathlib import Path
base_dir = Path(__file__).resolve().parent.parent  # Определяем корневую директорию проекта.
# Чтобы откуда бы мы не запустили скрипт, он точно знал где находится целевая папка
